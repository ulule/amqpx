machine:
  services:
    - docker
  environment:
    AMQPX_CLUSTER_MODE: yes

dependencies:
  override:
    - scripts/rabbitmq

test:
  override:
    - scripts/go-wrapper test
    - scripts/go-wrapper lint
