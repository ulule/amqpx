machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose==1.5.2
    - docker-compose -f docker-compose.yml build
    - docker-compose -f docker-compose.yml run -d rabbitmq

test:
  override:
    - docker-compose -f docker-compose.yml run test
