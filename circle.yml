machine:
  services:
    - docker

dependencies:
  environment:
    AMQPX_CLUSTER_MODE: yes
  override:
    - scripts/rabbitmq

test:
  override:
    - scripts/go-wrapper test
    - scripts/go-wrapper lint
